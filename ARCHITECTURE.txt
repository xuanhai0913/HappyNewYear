┌─────────────────────────────────────────────────────────────────────┐
│                     🎆 HAPPY NEW YEAR 2026 ARCHITECTURE             │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   USER BROWSER  │
└────────┬────────┘
         │
         │ 1. Visit website
         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         VERCEL CDN (Static)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │index.html│  │  xh.html │  │  CSS     │  │    JS    │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
└─────────────────────────────────────────────────────────────────────┘
         │
         │ 2. Load JavaScript
         │
         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     FRONTEND (Browser JS)                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                         │
│  │ api.js   │  │ lixi.js  │  │  xh.js   │                         │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘                         │
└───────┼─────────────┼─────────────┼───────────────────────────────┘
        │             │             │
        │ 3. API calls
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────────┐
│              VERCEL SERVERLESS FUNCTIONS (Backend)                  │
│  ┌────────────────────────┐  ┌─────────────────────────┐           │
│  │  /api/visitor-count    │  │     /api/lixi           │           │
│  │  ────────────────────  │  │  ─────────────────────  │           │
│  │  - GET: Get count      │  │  - GET: Leaderboard    │           │
│  │  - POST: Increment     │  │  - GET: Stats          │           │
│  │                        │  │  - POST: Save lixi     │           │
│  └───────┬────────────────┘  └──────────┬──────────────┘           │
└──────────┼───────────────────────────────┼──────────────────────────┘
           │                               │
           │ 4. Database operations        │
           │                               │
           ▼                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      VERCEL KV (Redis Database)                     │
│  ┌──────────────────────────────────────────────────────┐           │
│  │  KEYS:                                               │           │
│  │  ─────────────────────────────────────────────       │           │
│  │  • visitor_count          → Total visitors           │           │
│  │  • visitor_today          → Today's visitors         │           │
│  │  • last_reset            → Last reset date          │           │
│  │  • lixi_leaderboard      → Array of top players     │           │
│  │  • total_lixi_given      → Total lucky money count  │           │
│  │  • total_amount_given    → Total amount distributed │           │
│  │  • total_players         → Total unique players     │           │
│  └──────────────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
                          DATA FLOW EXAMPLES
═══════════════════════════════════════════════════════════════════════

📊 VISITOR COUNTER FLOW:
──────────────────────
User visits → JavaScript (api.js) → POST /api/visitor-count 
→ KV incr('visitor_count') → Return {total, today} → Display in header

🧧 LIXI GAME FLOW:
─────────────────
User plays → JavaScript (xh.js) → Random amount → POST /api/lixi 
→ Save to KV → Update leaderboard → Return rank → Show result + rank

🏆 LEADERBOARD FLOW:
───────────────────
Page load → JavaScript → GET /api/lixi?action=leaderboard 
→ Get from KV → Sort by amount → Return top 10 → Display

═══════════════════════════════════════════════════════════════════════
                            FILE STRUCTURE
═══════════════════════════════════════════════════════════════════════

HappyNewYear/
├── 📁 api/
│   ├── visitor-count.js    ← Serverless function for visitor tracking
│   └── lixi.js            ← Serverless function for lixi management
├── 📁 js/
│   ├── api.js             ← API client helper
│   ├── lixi.js            ← Lixi form logic
│   └── xh.js              ← Lixi game logic + animations
├── 📄 index.html          ← Homepage (with visitor counter)
├── 📄 xh.html             ← Lixi game page (with leaderboard)
├── 📄 vercel.json         ← Vercel configuration
├── 📄 package.json        ← Dependencies (@vercel/kv)
├── 📄 QUICKSTART.md       ← Quick deployment guide
└── 📄 DEPLOY.md           ← Detailed deployment guide

═══════════════════════════════════════════════════════════════════════
                        VERCEL KV DATA MODEL
═══════════════════════════════════════════════════════════════════════

VISITOR DATA:
├── visitor_count: 1234           (Integer)
├── visitor_today: 56             (Integer)
└── last_reset: "2025-12-06"      (String)

LIXI DATA:
├── lixi_leaderboard: [           (Array)
│     {
│       id: 1735689600000,
│       name: "Nguyen Van A",
│       amount: 1000000,
│       ageGroup: "adult",
│       timestamp: "2025-01-01T00:00:00.000Z"
│     },
│     ...
│   ]
├── total_lixi_given: 100         (Integer)
├── total_amount_given: 50000000  (Integer)
└── total_players: 75             (Integer)

═══════════════════════════════════════════════════════════════════════
                          KEY FEATURES
═══════════════════════════════════════════════════════════════════════

✅ Real-time visitor tracking (total + today)
✅ Persistent lucky money records in Redis
✅ Top 10 leaderboard (sorted by amount)
✅ Statistics dashboard (total gifts, amount, players)
✅ User ranking after receiving lucky money
✅ Automatic daily reset for "today" counter
✅ Form validation (name + age group required)
✅ Beautiful animations and effects
✅ Mobile responsive design
✅ Fast global CDN delivery via Vercel

═══════════════════════════════════════════════════════════════════════
